services:
  - type: web
    name: atopic-slide-api
    runtime: python
    buildCommand: pip install -r requirements.txt && bash setup_fonts.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
